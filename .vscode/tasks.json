{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "Delete and Restart Docker Setup",
			"command": "docker-compose down && docker system prune -f && docker-compose up -d",
			"isBackground": false
		}
	]
}